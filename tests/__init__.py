# Convertir el directorio de tests en un paquete importable
# Este fichero permite a unittest importar el paquete de tests cuando se
# usa discovery con -s tests o cuando el directorio se añade al sys.path.
